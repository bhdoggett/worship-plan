'use client';

import Service from "./service-entire";
import Link from "next/link";

const AllServices = ({children}) => {
  const [allServices, setAllServices] = useState()
  return (allServices.map((service) => 
  <);
}
 
export default AllServices;
